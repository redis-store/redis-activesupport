language: ruby
script: 'bundle exec rake'
rvm:
  - 2.2.0
  - ruby-head
  - rbx-19mode
  - jruby-19mode
  - jruby-head
services:
  - redis-server --port 6380 --requirepass 'password'
matrix:
  allow_failures:
    - rvm: jruby-head
    - rvm: ruby-head
